import { BlcClickDirective } from "./blc-click.directive";

describe("BlcClickDirective", () => {
	it("should create an instance", () => {
		const directive = new BlcClickDirective();
		expect(directive).toBeTruthy();
	});
});
