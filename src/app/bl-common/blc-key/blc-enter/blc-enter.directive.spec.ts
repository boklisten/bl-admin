import { BlcEnterDirective } from './blc-enter.directive';

describe('BlcEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new BlcEnterDirective();
    expect(directive).toBeTruthy();
  });
});
